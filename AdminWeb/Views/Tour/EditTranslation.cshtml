@model AdminWeb.Models.TourTranslation

@{
    ViewData["Title"] = "Sửa bản dịch Tour";
}

<div class="page-title">Sửa bản dịch Tour</div>
<div class="page-subtitle">Cập nhật nội dung ngôn ngữ <strong>@Model.LanguageCode.ToUpper()</strong>.</div>
<div class="card" style="padding:28px;">
    <form asp-action="EditTranslation" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="TourId" />
        <input type="hidden" asp-for="LanguageCode" />
        
        <div class="form-grid">
            <div class="form-group full">
                <label class="form-label">Tên Tour</label>
                <input class="form-control" asp-for="Title" type="text" required />
            </div>
            <div class="form-group full">
                <label class="form-label">Mô tả Tour</label>
                <textarea class="form-control" asp-for="Description" rows="5"></textarea>
            </div>
        </div>
        <div style="display:flex;gap:12px;flex-wrap:wrap; margin-top:24px;">
            <button type="submit" class="btn btn-primary"><i class="fa-solid fa-floppy-disk"></i> Cập nhật</button>
            <a asp-action="Translations" asp-route-id="@Model.TourId" class="btn btn-secondary">Quay lại</a>
        </div>
    </form>
</div>